# BST 
## Binary Search Tree

### Binary Search + Binary Tree

## Rules of BST 
1. All values of left sub-tree < (less than) node->val
2. All values of right sub-tree > (greater than) node->val
3. equal val should not be insert but in need to be counted