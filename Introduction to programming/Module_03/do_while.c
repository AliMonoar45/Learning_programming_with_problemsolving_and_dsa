#include <stdio.h>

int main()
{
    // int i = 1;
    // do
    // {
    //     printf("%d\n", i);
    //     i++;
    // } while (i <= 10);

    /*
    do while loop is also do the same as the other loops but it has some difference like if we need where the condition is true or false at least the loop runs once in that case we can use do while loop
     */
    int i = -5;
    while (i<3)
    {
        printf("hello " );
        i += 2;
    }
    
    return 0;
}