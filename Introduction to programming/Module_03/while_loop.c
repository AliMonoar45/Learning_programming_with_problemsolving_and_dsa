#include <stdio.h>

int main()
{
    int i = 1;
    while (i <= 10)
    {
        printf("%d\n", i);
        i++;
    }

    return 0;
}

/*
we can use while loop in various time it also does the same as for loop whatever we can achieve in for loop we can also use while to achieve that thing but when we are coding there might be some case where we use those different loop for better understanding 
 */