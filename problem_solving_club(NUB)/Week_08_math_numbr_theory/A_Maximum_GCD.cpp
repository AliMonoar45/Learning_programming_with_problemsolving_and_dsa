#include <bits/stdc++.h>
using namespace std;
void solve()
{
    int n;
    cin >> n;
    cout << n / 2 << endl;
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int tc;
    cin >> tc;
    while (tc--)
    {
        solve();
    }

    return 0;
}