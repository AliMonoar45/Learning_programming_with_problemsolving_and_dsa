#include <bits/stdc++.h>
using namespace std;
void solve(){
    int n;

    cin >> n;
    string s, t, m;
    cin.ignore();
    cin >> s ;
    cin.ignore();
    cin >> t;
    cin.ignore();
    cin >> m;
    cout << s << endl
         << t << endl
         << m << endl;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int tc;
    cin >> tc;
    while (tc--)
    {
        solve();
    }
    
    return 0;
}