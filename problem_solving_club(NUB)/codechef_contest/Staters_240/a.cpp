#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long x, y;
    cin >> x >> y;
    long long total = (1LL * x * 5000) + (1LL * y * 9800);
    cout << total;
    return 0;
}